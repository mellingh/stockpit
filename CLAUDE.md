# CLAUDE.md — Projektkontext Stockpit

Lokales Aktien-Portfolio-Dashboard für Micha (GitHub: mellingh). Deutsch als UI- und Doku-Sprache.
App-Name: **Stockpit** (Stock + Cockpit; die früheren Namen "Aktien-Cockpit" und "Kurswerk"
hat Micha verworfen — Repo und Ordner heißen ebenfalls stockpit).

## Harte Anforderungen (nicht verhandelbar)

- **Null laufende Kosten**: keine Abos, keine API-Keys, keine Tokens, kein Login — auch nicht optional.
- Läuft **komplett lokal** (Node.js-Server + Browser), muss per Ordner-Kopie/Klon + `start.bat` weitergebbar sein (Zielgruppe: auch Michas Vater, kein Techniker).
- Persönliche Daten (`data/portfolio.json`) bleiben lokal und sind gitignored — niemals committen.

## Stack & Architektur (bewusst minimal)

- **Kein Framework, kein Build-Schritt**: Express-Server (`server/`) + drei statische Vanilla-JS-Seiten (`public/`), ES-Module direkt im Browser.
- Start: `npm start` oder `start.bat` (prüft Node, installiert bei Erstlauf) → http://localhost:3001
- `server/index.js` = alle API-Routen; Module: `yahoo.js` (Daten), `indicators.js` (SMA/RSI/MACD/Perf), `news.js` (RSS+Ticker-News), `sentiment.js` (lokale KI), `analysis.js` (Kategorisierung, Betroffenheits-Mapping, Relevanz-Filter, Erklärtexte, Gesamt-Score), `trials.js` (clinicaltrials), `calendar.js` (Wirtschaftskalender), `storage.js` (JSON-Datei), `cache.js` (TTL-Cache gegen Rate-Limits).

## Datenquellen (alle kostenlos, ohne Key)

| Quelle | Wofür | Achtung |
|---|---|---|
| `yahoo-finance2` **v4** | Kurse, Historie, Fundamentaldaten, Analysten (`recommendationMean` = Finviz-Recom 1–5, `recommendationTrend`, `upgradeDowngradeHistory`), Termine, Suche, FX | v4 ist **Klassen-API**: `new YahooFinance({suppressNotices: [...]})` — nicht das v2-Singleton-Muster verwenden |
| `Xenova/finbert` via `@huggingface/transformers` | News-Sentiment, lokal, Download einmalig nach `models/` (gitignored) | englisch; dt. Fallback `Xenova/bert-base-multilingual-uncased-sentiment`; Modelle per env `SENTIMENT_MODEL_EN`/`_MULTI` tauschbar |
| RSS (CNBC, MarketWatch, tagesschau) | Makro-News | Liste in `news.js` `MACRO_FEEDS` |
| clinicaltrials.gov API v2 | Studien bei Healthcare-Aktien | Sponsor-Suche ist wortbasiert („Moderna“ findet „ModernaTX“ nicht) → Fallback über `query.term` + Sponsor-Filter ist in `trials.js` eingebaut |
| investing.com-Widget (`sslecal2.investing.com`, lang=8=Deutsch) | Wirtschaftskalender **mit Aktuell-Werten** | blockt Node-Fetch per TLS-Fingerprint (403) → Server ruft `curl` auf (ab Win10 vorinstalliert); Fallback: ForexFactory-Feed (`nfs.faireconomy.media/ff_calendar_thisweek.json`, ohne Aktuell) |
| Google-Translate-Endpunkt (`translate.googleapis.com/translate_a/single?client=gtx`) | "Worum es geht"-Teaser → Deutsch | inoffiziell wie Yahoo, kein Key; pro Text 1 Tag gecacht (`uebersetzen()` in news.js), Fehler → Original |
| stockanalysis.com (`/stocks/{ticker}/ratings/`, serverseitig gerendert) | Analysten-Ratings **mit Kursziel je Bank** | nur US-Ticker (mit Suffix → Yahoo-Fallback ohne Kursziele); Spalten von hinten parsen (vorne Mobil-Duplikate) |
| TradingView Lightweight Charts (aus node_modules über `/vendor` serviert) | Candlestick-Chart | Marker pro Tag bündeln (sonst stapeln sie sich) |

## Produktentscheidungen (nicht rückgängig machen / nicht wieder vorschlagen)

- **X/Twitter-Experten-Modul wurde entfernt** (kostenloses Mitlesen unzuverlässig, Nitter geblockt). Nicht wieder einbauen, auch keine Bild-Auswertung von Posts (bräuchte Vision-Modell — als zu aufwendig verworfen).
- **News-Relevanz-Filter** (`analysis.js` → `isRelevant`): Portfolio-/Watchlist-News immer und zuerst; Makro nur bei Marktbewegern (Fed/EZB/Zins/Inflation/Geopolitik/Präsident); Fokus-Sektoren **Fintech, Biotech, Tech** (Michas Schwerpunkte); Ratgeber-/Boulevard-Artikel raus.
- Kursziele je einzelner Bank gibt es kostenlos nicht → nur Konsens-Spanne + Rating-Historie je Bank zeigen; ehrlich kommunizieren.
- Gesamteinschätzung = Technik + Analysten + News-Sentiment, jede Komponente offen begründet (keine Blackbox).
- **Snowflake-Analyse** (`server/snowflake.js`, SWS-Stil): 5 Dimensionen (Wert/Zukunft/Vergangenheit/Bilanz/Dividende) je 0–5, regelbasiert, plus Stärken/Risiken in Klartext. **Financial Services**: Bilanz über ROE/Marge bewerten (D/E, Current Ratio, FCF fehlen bei Banken strukturell) und nie als Schwachpunkt ausrufen, wenn Datenlage fehlt — in der Übersichts-Querkarte der Analyse-Seite mit SVG-Radar (`radarChart` in ui.js).
- Dashboard-Allokation gruppiert nach **Sektor** (ETFs eigener Topf), nicht nach Einzelwerten. Die frühere Klumpenrisiko-Warnung wurde auf Michas Wunsch **komplett entfernt** — nicht wieder einbauen.
- "Wichtige Termine": zwei kompakte Spalten (Deine Werte | Markt-Events), Überschriften neutral grau, kein rotes "heute", kleine Chips (`chip-sm`), Kernwerte direkt in der Zeile (EPS farbig nach Vorzeichen, Prognose), kein Aufklappen — Zeilen verlinken zu Analyse bzw. Kalender.
- Platzsparen mit `<details>`-Akkordeons (`collapsible()` in ui.js) — Micha mag aufklappbare Elemente; nichts darf horizontal scrollen (Seite max 1440px, 3-spaltiges Grid). Aufklapp-Indikator: klassisches SVG-Chevron (`chevronIcon()` in ui.js), gut sichtbar. **Keine Doppellinien** (Runde 17): `details.clap` hat zwar global eine border-top, aber INNERHALB einer News (Einordnung) und direkt NACH einer News ("Mehr anzeigen") ist sie entfernt — getrennt wird nur zwischen News über die border-bottom der `.news-item`.
- **Keine nackten Score-Zahlen im UI** (Micha-Feedback): Gesamteinschätzung und Technik zeigen nur das farbige Urteil — Labels auf ENGLISCH: **Bullish/Neutral/Bearish** (`AMPEL_TEXT` in ui.js, Micha-Wunsch Runde 11, "Bärisch" klang falsch) — keine Skalen, keine "+28"-Werte, keine Begründungs-Panels.
- News-Darstellung (Runde 8): Ticker-Chips OBEN in der Meta-Zeile, dann großer Titel, dann Badge-Zeile (`newsBadgesRow()`) OHNE %-Zahl (verwirrte Micha), darunter aufklappbare "Einordnung" (`newsEinordnung()`) — seit Runde 11 als knappe Bullet-Liste (`ul.einordnung-liste`), Sätze kommen als Array aus `explain()` in analysis.js. Volumen dort als "Handelsvolumen" bezeichnen (nicht "Umsatz" — verwechselbar mit Erlösen). **Keine generischen Bullets** (Runde 12): Signalwort-/KI-Sicherheits-Satz und Kategorie-Boilerplate sind bewusst raus — nicht wieder einbauen. Stattdessen "Worum es geht:"-Teaser: aus der RSS-Description, und wo die fehlt (Yahoo-Ticker-News) wird die og:description der Artikelseite nachgeladen (`getArticleSummary`/`fillSummaries` in news.js — via **curl**, weil finance.yahoo.com Nodes fetch-TLS-Fingerprint blockt wie investing.com; pro URL 1 Tag gecacht).
- News-Feed (Runde 12): nichts älter als 7 Tage, max. 20 Einträge. Sobald Positionen/Watchlist existieren, ist der Feed streng (`isRelevant(item, hatWerte)`): nur direkte News + MARKET_MOVERS; SECTOR_FOCUS-News nur bei leerem Depot.
- **Pre-/After-Market** (Runde 12): `ausserboerslich(quote)` in index.js (marketState PRE*/POST*/CLOSED + pre/postMarketPrice) → Analyse-Kopf (`#r-prepost`, "Pre-Market"/"Nachbörslich") und als `.prepost-mini`-Zweitzeile in der Heute-Spalte von Positionen/Watchlist. Nur US-Börsen liefern das; XETRA & Co. → null, Anzeige entfällt still.
- **"Meinungen auf X"-Karte** auf der Analyse-Seite (Runde 12, `renderX` in analyse.js, Panel `#p-x` neben Analysten): KEIN Scraping/keine API — nur vorgefertigte Links `x.com/search?q=from:HANDLE $TICKER&f=live`. Optik (Runde 18): Titel "Meinungen auf [X-Logo-SVG]" einzeilig (nowrap), Subline `.x-sub` darunter, pro Account ein prominenter `a.x-link`-Button (Logo + @handle + $TICKER ↗ rechts, Hover: Accent-Rand + translateX) — kein langer Hinweistext mehr in der Karte.
- **Frische Quartalszahlen** (Runde 18, `frischeZahlen()` in index.js): zweistufig — `gemeldet` kommt aus `quote.earningsTimestamp` (≤14 Tage in der Vergangenheit), das Ist-EPS aus `earningsHistory` (jüngste Zeile zählt nur, wenn ihr Quartalsende ≤60 Tage her ist; Yahoo trägt das Ist oft erst Stunden nach dem Report nach → bis dahin "Ergebnis folgt"). Anzeige: `.zahlen-chip` unterm Analyse-Titel (Ist vs. erw. + Überraschungs-%, pos/neg getönt) und im Dashboard-Termin-Panel (gemeldete Zahlen der letzten 3 Tage bleiben als Zeile stehen, `gemeldetGezeigt` verhindert Dubletten mit dem Kalender-Termin). Markt-Events reichen `aktuell`/`aktuellTrend` durch → "Ist x" grün/rot (nur wenn investing.com-Quelle liefert, ForexFactory-Fallback hat kein Aktuell). Accounts (Standard-Trio: TheLongInvest, Biotech2k1, thestockwhale) liegen in portfolio.json (`xAccounts`), verwaltbar über `/api/xusers` (GET/POST/DELETE, Handle-Regex `^[A-Za-z0-9_]{1,15}$`). Karte heißt seit Runde 24 **"Meinungen & Links"**, seit Runde 25 als **kompakte `.x-pill`-Pills** (flex-wrap, nur Icon + Name — X-Logo bzw. Globus, KEIN Cashtag im Label, Ziel-Info im Tooltip). Verwaltung (Runde 26): Ghost-Button **"+ Hinzufügen"** unter den Pills → zentriertes **Modal** (`.xl-modal`, nutzt .modal-overlay/.modal des Bestätigungsdialogs; Esc/Overlay-Klick/✕ schließt; `renderX(a, true)` öffnet es nach Änderungen neu). Verworfen: Akkordeon (R21), +-Knopf im Titel + Karten-Overlay (R24), +-Pille (R25) — Karte muss kompakt bleiben (~220px).
- Termin-Zeilen: "EPS erw." IMMER neutral grau (wie "Prog." bei Markt-Events), Farbe bekommt nur der Ist-Wert (Überraschung). Der "n unwichtige Meldungen ausgefiltert"-Satz unter der News-Lage wurde entfernt (Runde 26) — nicht wieder einbauen.
- Dashboard-Footer minimal (Runde 25): nur noch "Stockpit läuft komplett lokal · Alle Angaben ohne Gewähr — keine Anlageberatung" — keine Daten-/Quellenliste im UI (steht im README).
- **Keine Fremdseiten-Referenzen in Überschriften/Hints** (Runde 24, wegen möglicher Vermarktung): kein "meistgesucht bei Yahoo", kein "Quelle: stockanalysis.com", keine Kalender-Quellen-Zeile. Quellen stehen ehrlich im README und im Seiten-Footer. Nicht verwechseln mit dem früheren Experten-Modul (Sentiment-Gewichtung) — das bleibt entfernt. Seit Runde 21 zusätzlich **Web-Quick-Links** ("Direkt öffnen"): `webLinks` in portfolio.json mit `{TICKER}`-Platzhalter (Defaults: Yahoo `quote/{TICKER}`, SimplyWallSt `stocks?search={TICKER}` — Firmen-URLs sind nicht templatebar!, TradingView `chart/?symbol={TICKER}`, Finviz `quote.ashx?t={TICKER}`), API `/api/weblinks` (GET/POST/DELETE?url=). Das EINE Verwalten-Eingabefeld erkennt selbst: `@handle`/nacktes Handle → X-Account, alles andere → URL (https:// wird ergänzt; kommt das aktuelle Symbol mit Wortgrenzen in der URL vor, wird es zu `{TICKER}`).
- **Kalender-Zeilen sind aufklappbar** (Runde 21): Klick → `tr.cal-detail` mit Erklärung aus `EVENT_LEXIKON` in kalender.js (Regex-Muster für dt. UND engl. Titel: CPI/PCE/BIP/NFP/PMI/Zinsentscheid/…, je `was` + `deutung`-Lesart + optional `richtung` ('hoch-gut'/'hoch-schlecht') für die **Devisen-Faustregel** ("höher als erwartet stärkt i. d. R. die Landeswährung …", investing.com-Stil), generischer Fallback, Ergebnis-Zeile grün/rot. Erweiterungen bitte dort ins Lexikon.
- **Länderkürzel statt Währung** (Runde 23): calendar.js parst das Land aus der investing-Flaggen-Klasse (`ceFlags United_States` → `LAND_AUS_NAME`), Fallback `LAND_AUS_WAEHRUNG` (ForexFactory kennt nur Währungen; EUR → 'EU'). Kalender zeigt Flaggen-Emoji (aus ISO-Code generiert) + Kürzel, Dashboard-Markt-Chips nur das Kürzel (Währung im Tooltip).
- **Analyse-Startansicht** (Runde 23, `#start` + `renderStart()` in analyse.js): ohne `?symbol` zwei Panels — "Deine Werte" (Positionen+Watchlist dedupliziert) und "Gerade im Trend" (`/api/trending` → `yahooFinance.trendingSymbols('US')`, nur EQUITY/ETF, max 8) als klickbare `.start-chip`s (Symbol, Name, Tages-%); Klick lädt den Report und versteckt die Startansicht.
- **Weblink-Muster** (Runde 23, `SITE_PATTERNS`/`sitePattern()` in storage.js): URLs OHNE {TICKER} (eingefügte Startseiten) werden beim POST über bekannte Hostnamen-Muster repariert (seekingalpha→`/symbol/{TICKER}`, stockanalysis, stocktwits, marketwatch, zacks, …); unbekannte Hosts → 400 mit Anleitung ("Aktien-Seite der URL einfügen, Symbol wird zum Platzhalter"). `storage.load()` migriert Alt-Einträge ohne Platzhalter über dieselben Muster.
- Einordnung-Analysepunkte (Runde 21, `explain()`/`priceReaction()`): zusätzlich Vorwochen-Trend (±3 %-Schwelle, "traf auf starke/schwache Phase") und Abstand zum 52-Wochen-Hoch (≤−30 % "Erwartungen gedrückt" / ≥−5 % "viel Optimismus eingepreist"; bei kurzer Historie heißt es "Hoch der letzten Monate"). "Worum es geht"-Teaser werden serverseitig ins Deutsche übersetzt.
- **Desktop-Verknüpfung**: `verknuepfung-erstellen.bat` legt per PowerShell/WScript.Shell eine "Stockpit.lnk" auf dem Desktop an, die start.bat aus dem jeweiligen Ordner startet (feste .lnk kann nicht mitgeliefert werden — absolute Pfade).
- Markt-Event-Zeilen: **USD/EUR als Text-Chip** (`.chip-sm.cur`), KEINE Flaggen-Emojis (im Dark Mode kaum erkennbar); Kalender-Tabelle behält Flagge+Code. Zwischen zwei Kernwerten einer Termin-Zeile (EPS erw. | Ist, Prog. | Ist) trennt eine Linie (`.eps + .eps` border-left).
- Dashboard-Eigenheiten: `set()` für Hero-KPIs muss pos/neg/dim IMMER zurücksetzen (sonst klebt Grau nach Löschen+Neuanlegen); der Zeilen-Editor (`editPosition`) blendet das `thead` aus, `loadDashboard()` baut es wieder auf.
- Dashboard hat **Inline-Hinzufügen** (Runde 13, ersetzt die +-Knöpfe im Titel — die fand Micha unübersichtlich): Ghost-Button "+ Position hinzufügen" / "+ Wert beobachten" UNTER der Tabelle, Klick klappt das Formular auf (Button wird zu "Schließen"), nach dem Anlegen klappt es zu (`bindAddForm` in dashboard.js). Alle Formular-Elemente auf 42px-Höhe (`.inline-add input/.btn`), damit nichts versetzt wirkt. Die Portfolio-Seite bleibt trotzdem — sie ist der Ort für Ändern/Löschen. Zeilen in Positionen/Watchlist (Portfolio-Seite) sind klickbar (`tr.rowlink` → Analyse), Buttons in den Zeilen stoppen den Klick mit `stopPropagation`.
- Sentiment-Ausfälle nie cachen: `classifyCached` in index.js wirft `unavailable`-Ergebnisse per `uncache()` sofort wieder aus dem Cache, `getPipeline` in sentiment.js lädt single-flight — sonst klebt "KI-Modell noch nicht geladen" 24 h an einer Schlagzeile.
- Chart im TradingView-Stil: Zeiträume 1T/1W (Intraday via `getIntraday`) + 1M/6M/1J/5J/Max, OHLC-Zeile oben links folgt dem Fadenkreuz, Rad-Zoom + Achsen-Drag aktiv. Preislinien (Runde 13, `setPriceLines` in analyse.js): Vortagsschluss grau gepunktet, Vorbörslich/Nachbörslich orange gestrichelt. **Mausrad über der Preisachse zoomt die Preise** — Lightweight Charts kann das nicht nativ; gelöst über `autoscaleInfoProvider` + `priceZoom`-Faktor und einen Capture-Wheel-Listener, der nur im Achsenbereich greift (Doppelklick auf die Achse = Reset; `priceZoom` bei Symbol-/Zeitraumwechsel auf 1). **News-Marker im Chart wurden komplett entfernt** (Micha-Feedback: verwirrend, TradingView hat sie auch nicht) — nicht wieder einbauen.
- Analyse-Layout (Runde 14, Yahoo-Stil): `.chart-row` = Chart links + 330px-Seitenspalte `.chart-aside` rechts, darin NUR noch **"Meinungen auf X"** (X-Links einzeilig, `white-space: nowrap`, als `a.btn.ghost` — .btn-Styles gelten für button UND a!). **Technik-Panel und Kennzahlen-Panel sind komplett entfernt** (Micha, Runde 14) — die Technik fließt weiter in die Gesamteinschätzung ein, die wertvollsten Kennzahlen (Perf. YTD/Jahr farbig, Short Float) stehen im Quote-Strip. Reihenfolge: Chart-Row, Quote-Strip, Übersicht, Grid (Analysten | Analysten-Historie), News GANZ unten. Historie + Dashboard-News-Feed zeigen max. 5 Einträge, Rest hinter "Mehr anzeigen (n)". Bei der Gesamteinschätzung kein "zur Einordnung ↓"-Text mehr (Klick springt weiterhin). Das "Accounts verwalten"-Akkordeon der X-Karte bleibt nach Hinzufügen/Entfernen OFFEN (`renderX(a, true)`).
- Chart-Overlay (Runde 14, TradingView-Stil): `.chart-overlay` oben links = Titelzeile `.chart-symline` ("Wert · Zeitraum · Börse", `updateChartTitle()` bei Symbol-/Zeitraumwechsel) + OHLC-Zeile darunter.
- **Portfolio-Seite ist GELÖSCHT** (Runde 14, portfolio.html/portfolio.js entfernt, Nav 3 Einträge): Anlegen über die "+ …"-Buttons, Ändern/Löschen über Hover-Icon-Knöpfe (`iconBtn` Stift/Kreuz, `.row-actions`, gedimmt bis Zeilen-Hover) direkt in den Dashboard-Tabellen; Bearbeiten ersetzt die Zeile durch einen Inline-Editor (`editPosition`). Löschen weiterhin ohne Rückfrage.
- Analyse-Suchfeld: Lupen-SVG + `.search-wrap.search-lg` (48px, padding-left), kurzer Placeholder "Aktie oder ETF suchen …" — keine langen Beispiel-Placeholder.
- Report-Kopf (Runde 25): Name + Badge-Zeile, SONST NICHTS — die **Gesamteinschätzungs-Anzeige (Bullish/Bearish) wurde KOMPLETT aus dem UI entfernt** (Micha, Runde 25; Server berechnet `gesamt` weiter, nichts zeigt es an — nicht wieder einbauen ohne Auftrag). Frische Quartalszahlen erscheinen als **`.zahlen-banner`** über der Chart-Karte (Yahoo-Stil, pos/neg getönt, "EPS erw. → Ist") und NUR am Meldetag (heute/gestern, `zTage <= 1`) — danach verschwindet der Banner; im Dashboard-Terminpanel bleiben sie 3 Tage. **Kurs oben LINKS in der Chart-Karte** (`.chart-kurs`: Preis + Tages-% + Pre-Market inline; `.prepost[hidden] { display:none }` ist Pflicht). `.chart-row` hat `align-items: start` — die Seitenspalte darf die Chart-Karte NICHT strecken (Runde 24, revidiert R15).
- Snowflake-Radar: `radarChart()` weitet die viewBox seitlich (±36px) und ankert seitliche Labels winkelabhängig (start/end) — sonst werden ZUKUNFT/DIVIDENDE abgeschnitten. Der **Fazit-Satz unterm Radar wurde entfernt** (Runde 22, Micha) — Server berechnet `fazit` noch, Frontend zeigt ihn nicht; nicht wieder einbauen.
- Kalender-Aufklapp-Details als knappe **Stichpunkte** (`ul.einordnung-liste`, Lesart-Sätze gesplittet), kein "Sterne = Marktwirkung"-Boilerplate. Simply-Wall-St-Quick-Link läuft über die eigene Redirect-Route `/api/goto/sws/:symbol` (relative URL im webLink-Template!): `swsFirmenPfad()` in index.js fragt die interne SWS-Such-API `simplywall.st/api/search/{q}` ab (kostenlos/inoffiziell, via curl, 1 Tag gecacht) und leitet auf die echte Firmen-URL weiter (Fallback: Google-Site-Suche) — funktioniert auch für SAP.DE etc.; alte gespeicherte URLs (SWS-Suche, Google-Suche) werden in `storage.load()` migriert.
- **Kennzahlen-Leiste unterm Chart auf ENGLISCH** (Runde 19, Micha will dort die Yahoo/Finviz-Fachbegriffe: Previous Close, PE Ratio (TTM), Forward PE, Revenue Growth, …) — NUR diese Leiste, der Rest der UI bleibt deutsch.
- **Fachbegriffe erklären** (Runde 19/20): Snowflake-Stärken/Risiken sind `{t, info}`-Objekte — `info` erklärt Abkürzungen (PEG, ROE, EPS, Free Cashflow, Short Float, Current Ratio …) laienverständlich, steckt aber NUR im Hover-Tooltip (`title`) des Punkts. Die sichtbaren Erklärzeilen aus Runde 19 fand Micha zu unübersichtlich — wieder entfernt, nicht wieder einbauen (Frontend akzeptiert weiterhin auch plain Strings). Report blendet gestaffelt ein (`#report > *` reportIn-Animation, prefers-reduced-motion beachtet). `.chart-aside`-Panels füllen die Chart-Höhe (flex).
- **Sektor & Branche deutsch** (Runde 14): `SEKTOR_DE`/`BRANCHE_DE` + `brancheDeutsch()` in index.js übersetzen NUR die Anzeige-Felder der Analyse-Response; intern (Snowflake `istFinanzwert`, CATEGORY_SECTORS-Mapping) bleiben Yahoos englische Originalwerte. Yahoo schreibt Branchen-Bindestriche mal "—" mal "-" → Lookup normalisiert.
- Fundamentaldaten haben kein eigenes Panel: Kernwerte im Kennzahlen-Strip unterm Chart, Rest im Kennzahlen-Akkordeon (ETF-Profil bleibt eigenes Panel).
- Seeking Alpha, nasdaq.com, nyse.com, justETF: geprüft und verworfen (Paywall/redundant).

## Stockpit v2 (Branch feature/react-shadcn-redesign)

- **Neues Frontend unter `web/`**: Vite 7 + React 19 + TypeScript (strict) + Tailwind v4 + shadcn-Stil-Komponenten (Quellcode in `web/src/components/ui/`, KEINE shadcn-CLI). Server (`server/`) blieb 1:1; Express liefert `web/dist/` mit SPA-Fallback NACH den /api-Routen. `start.bat` installiert/baut die Oberfläche beim Erststart. `public/` bleibt bis zur Abnahme als Referenz liegen.
- **Kein react-router**: bewusst entfernt (Audit-Altlasten in SSR-Features + 16 KB) — Mini-Router in `web/src/lib/router.tsx` (`Link`, `useNavigate`, `useSearchParams`, `usePathname`); alte `.html`-URLs bleiben gültig. `npm audit` im web/ ist SAUBER (0) und soll es bleiben.
- **Datenfluss**: typisierter API-Client (`lib/api.ts`, Typen = exakte Server-JSONs) + TanStack-Query-Hooks (`lib/queries.ts`); Portfolio-/Links-Mutations invalidieren automatisch — keine manuellen Reload-Ketten mehr.
- **Design-System „Terminal Luxe"** (`web/src/index.css` @theme) — die Regeln stehen als Kommentar oben in der Datei und sind VERBINDLICH:
  - **Höhen NUR aus der Skala** (8px-Raster): `h-control-xs` 24px (Badges/statische Chips), `h-control-sm` 32px (Pills, kleine Buttons, Icon-Buttons, Nav), `h-control-md` 40px (Standard-Buttons + Inputs), `h-control-lg` 48px (große Suche). Keine `h-7`/`py-2.5`-Eigenbauten mehr — Micha hatte zu Recht 28/31/32-Wildwuchs moniert.
  - **Schriften NUR aus der Skala**: `text-micro` 12px (Uppercase-Labels, Badges, Spaltenköpfe), `text-small` 13px (Mono-Werte, Hilfstexte), `text-base` 15px (Basis), `text-lg` 16px (News-Titel); Display 20–40px. **Nichts unter 12px** (vorher gab es 10,5/11/11,5 — auf 27″ Full HD zu klein).
  - **Palette aufgehellt** (Runde 1): bg #101420, panel #171d2b, panel2 #1e2536, elevated #252d41, line #2a3346, Akzent #6ba5ff, up #35d99a / down #ff6b78 NUR für Markt. Alle Textfarben ≥ 4,5:1 Kontrast auf Panel (nachgemessen: 14,9 / 8,8 / 5,4 / 6,8).
  - Fonts lokal (Bricolage Grotesque = Display, Schibsted Grotesk = Body, Red Hat Mono = Kursdaten), Glas-Panels mit Hairline + Innen-Highlight, `animate-rise`-Stagger. Die Linie unter der Topbar ist **statisch** — die animierte Version wirkte wie ein Ladebalken (nicht wieder einbauen).
- **Suche ist ein normales Eingabefeld** mit Vorschlagsliste darunter (`components/symbol-search.tsx`, combobox/listbox-Rollen, Pfeiltasten/Enter/Escape) — KEIN Modal/Command-Palette mehr (Micha, Runde 1).
- **Externe Links** (Meinungen & Links) immer mit ↗-Icon + `aria-label` „… öffnet in neuem Tab" + `rel="noopener noreferrer"`. Im Verwalten-Dialog wird per dezentem ✕-Icon entfernt (nicht per roter Button-Kolonne). Fachbegriff-Tooltips hängen am gestrichelt unterstrichenen Begriff selbst (`side="top" align="start"`), nicht am ganzen Block.
- **Installierte Skills** (`.claude/skills/`, aus vercel-labs/agent-skills + anthropics/skills): `web-design-guidelines` (UI-Audit gegen die Web Interface Guidelines — lädt die Regeln live von GitHub), `react-best-practices` (70 Performance-Regeln von Vercel), `composition-patterns`, `webapp-testing`, `theme-factory`. Vor Design-/Performance-Arbeit an v2 diese Skills nutzen.
- **Guideline-Konformität** (Audit-Runde): keine `transition-all` (nur benannte Properties), `:focus-visible` statt `:focus` (Ring nur bei Tastaturbedienung — Micha will keinen Ring beim Klicken), Skip-Link + `main#inhalt`, `theme-color`, `touch-action: manipulation`, `aria-live` auf Fehler-/Leerzuständen, alle Icon-Buttons mit `aria-label`, externe Links mit `rel="noopener noreferrer"` + „öffnet in neuem Tab"-Label, `spellCheck={false}` + `autoComplete="off"` in Suchfeldern, `inputMode="decimal"` bei Zahlenfeldern, `text-balance` auf Überschriften, `content-visibility` für die lange Kalender-Tabelle (`.lange-liste`). **Bewusste Abweichung**: Löschen ohne Bestätigung (Micha-Wunsch) — die Guideline fordert eine Rückfrage.
- **Code-Splitting**: `stock-chart` und die Analyse-/Kalender-Seiten werden per `lazy()` nachgeladen — Hauptbundle 136 KB gzip statt 190 KB, der Chart (53 KB) lädt nur, wenn wirklich ein Symbol gewählt ist.
- **Alle v1-Produktentscheidungen gelten in v2 weiter** (nicht-wieder-einbauen-Liste, deutsche UI, englische Kennzahlen-Leiste, EPS erw. grau/Ist farbig, Löschen ohne Rückfrage, Einordnungs-Bullets ohne Floskeln, Kalender-Lexikon in `web/src/lib/event-lexikon.ts` — dort erweitern).

## Konventionen

- UI-Texte deutsch, `Intl`-Formatierung `de-DE`, EUR als Anzeigewährung im Dashboard (FX über Yahoo `USDEUR=X`).
- Design (Michas Wunsch, Feedback-Runde 3): **TradingView/Finviz Dark Mode** — neutrales Schwarz/Grau (#0b0e13), Blau als Akzent (#4c8dff), Grün/Rot nur für Markt, Gelb (--warn) nur für Ampel/Sterne. Sans (Instrument Sans) für alles inkl. großer Zahlen, Mono für Kursdaten — keine Serifen mehr. Fonts lokal in `public/fonts/`. Chart-Kategorienfarben sind eine validierte Palette — nicht frei ändern.
- Portfolio: **kein Kaufdatum** (bewusst entfernt — Micha kauft in Tranchen, es zählt nur Stückzahl + Ø-Kaufkurs).
- `replaceChildren()` nie mit möglichen `null`-Kindern aufrufen (rendert als Text "null") — `setChildren()`-Helfer in `analyse.js` nutzen.
- `attachSearch(input, …)` erst aufrufen, NACHDEM das Input im `.search-wrap` hängt (der Wrap wird zwar inzwischen lazy aufgelöst, aber die Reihenfolge bleibt Konvention) — der Watchlist-Bug in Runde 18 kam genau daher.
- Nie Browser-confirm()/alert(). Löschen/Entfernen passiert OHNE Rückfrage (Micha-Wunsch, Runde 9) — `confirmDialog()` in ui.js existiert für Fälle, wo eine Bestätigung wirklich nötig wäre. Eingabefelder: überall Placeholder; Höhen-Staffel (Runde 17, Micha fand 42px überall zu klobig): Analyse-Suche 44px, Inline-Formulare in Panels 38px, X-Karte 34px — Felder und Buttons einer Zeile IMMER exakt gleich hoch (Vorsicht: die globale `input[type=…]`-Regel steht spät im Stylesheet und schlägt gleichspezifische Klassen-Regeln — Attribut-Selektor mitschreiben).
- Analysten-Monatssäulen: Strong Buy OBEN (wie Yahoo); Technik-Signale sortiert grün → gelb → rot.
- Fehler-Zustände immer benutzerfreundlich abfangen (Feed down, Ticker unbekannt, offline) — App darf nie leer/kaputt aussehen.
- **Jedes klickbare Element braucht einen sichtbaren Hover-Zustand** (Runde 20): Buttons (auch `.btn.danger`!), Pills, Zeilen, Chips, Links. Bei neuen Bedienelementen Hover gleich mitliefern.
- Ehrliche Grenzen im UI/README benennen statt Features vorzutäuschen. Überall gilt: keine Anlageberatung.

## Verifikation nach Änderungen

Testfälle: US-Aktie (NVDA), deutsche Aktie (SAP.DE), Biotech (MRNA → Studien-Panel), ETF (VUAA.DE), Fintech (KLAR). Gemischtes EUR/USD-Depot → Gesamtwert plausibel. Indikatoren ggf. gegen Handrechnung prüfen (SMA = Schnitt der letzten n Schlusskurse).

## Windows-Eigenheiten (Michas Heim-PC)

- gh CLI liegt unter `C:\Program Files\GitHub CLI\gh.exe` (nicht im PATH).
- HTTPS-Push brauchte `git config http.sslBackend schannel`.
- Git-Identität pro Repo: mellingh + `159627479+mellingh@users.noreply.github.com`.
